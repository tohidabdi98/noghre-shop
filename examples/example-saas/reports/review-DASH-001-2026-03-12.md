# Review report — DASH-001 (PR #21)

- **Agent:** `rev-dash-001` (review)
- **Date:** 2026-03-12
- **Artifact:** branch `agent/impl-dashboard-003/DASH-001`, commits `a91c4de..6b0e9f2`, PR #21
- **Baseline:** `main` @ `4c8d120`
- **Verdict:** FAIL → `changes_requested`

## 1. Evidence re-run (not trusted, reproduced)

| Command | Result |
|---|---|
| `python scripts/tp.py validate` | exit 0 |
| `python scripts/verify.py` | exit 1 — `tests/dashboard/activity-error.test.tsx` fails |
| `python scripts/tp.py pr-check --base main` | exit 0 (ownership and conventions clean) |

## 2. Acceptance criteria

| Criterion | Verdict | Evidence |
|---|---|---|
| AC-1 attention above the fold at 1280×800 | PASS | `tests/dashboard/layout.test.tsx` + screenshot 1280 |
| AC-2 last 10 events with actor and time | PASS | `tests/dashboard/activity.test.tsx` |
| AC-3 empty state with one primary action | PASS | `tests/dashboard/empty.test.tsx` + screenshot |
| AC-4 six states reachable | NOT VERIFIED | only the default state has screenshots |
| AC-5 keyboard path on the attention list | NOT VERIFIED | no walkthrough evidence, no keyboard test |

## 3. Findings

| # | Severity | Location | Finding | Expected (reference) | Smallest fix | Owner |
|---|---|---|---|---|---|---|
| B1 | **Blocking** | PR body / `docs/ux/screens.md#screen-02` | loading, empty, error and success screenshots are missing; the error state is not even implemented (`role="alert"` absent) | all six states implemented and evidenced | wrap the activity section in an error boundary with `role="alert"` + retry, capture 4 screenshots | `impl-dashboard-003` |
| B2 | **Blocking** | `src/dashboard/AttentionList.tsx` | keyboard operation unverified; list items are not focusable with a visible focus ring | `NFR-A11Y-1`, `UX-G-001` | make items focusable, add a keyboard test, record the walkthrough | `impl-dashboard-003` |
| NB1 | Non-blocking | `src/dashboard/ActivityFeed.tsx` | "time ago" uses `Date.now()` directly | conventions §3 (inject the clock) | inject the clock; file an issue if deferred | `impl-dashboard-003` |
| S1 | Suggestion | `src/dashboard/Dashboard.tsx` | the greeting could be memoised | — | optional | — |

## 4. Contract conformance

| Item | Result |
|---|---|
| provided interfaces | none (leaf module) — consistent with the contract |
| consumed interfaces | `IF-PROJECT-READ`, `IF-USER-PORT` used as specified; `null` user handled |
| ownership | diff ⊆ `allowed_to_modify`; no writes to `src/projects/**` or the route registry |
| tests | 18 pass, 1 fails; no weakened assertions found |

## 5. Required before merge

1. Implement the activity error state and make the failing test pass.
2. Attach screenshots for loading / empty / error / success at 375 / 768 / 1280.
3. Add a keyboard test for the attention list and record the manual walkthrough.
4. Re-request review from `rev-dash-001` on the new commits.

## 6. Recommended state transition

`changes_requested` → `in_progress` (already applied) → `awaiting_review` after the four items above.
